This is a neural network implementation in Python. The main file to run is
neural.py, which creates a sample network using random initial weights for each
arc and then prints the nodes to standard out.
